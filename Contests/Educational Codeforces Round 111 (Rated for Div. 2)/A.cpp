#include <bits/stdc++.h>
using namespace std;
#define mp             make_pair
typedef long long      ll;


void sol(){
      ll p, f2;
      double lk, rk;
      cin>>p;
      lk = sqrt(p);
      cout<<ceil(lk)<<"\n";
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin>>t;
    while(t--){
        sol();
    }
	return 0;
}