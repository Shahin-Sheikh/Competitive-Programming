#include <bits/stdc++.h>
using namespace std;

void run_case(){
    int n;
    cin>>n;
    int ara[100];
    for(int i=0;i<n;i++){
        cin>>ara[i];
        if(ara[i]==ara[])
    }
}
int main()
{
  ios_base::sync_with_stdio(0);cin.tie(0);
  int tests; 
  cin>>tests;
  while (tests--)
    run_case();
  return 0;
}