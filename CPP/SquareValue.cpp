#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, bign = 0, smaln = 0;
    cin >> n;
    bign = (n + 1) / 2;
    smaln = (n - 1) / 2;
    cout << smaln << " ";
    cout << bign;
    return 0;
}